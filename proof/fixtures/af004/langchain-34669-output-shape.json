{
  "id": "langchain-34669",
  "source_url": "https://github.com/langchain-ai/langchain/issues/34669",
  "source_title": "MCP tools return list instead of string",
  "component": "bounded",
  "pattern": "Tool returns list[dict] when downstream expects a structured record — output shape mismatch.",
  "violation": "output_validation_failed",
  "tool_name": "mcp_search",
  "schema_fields": {
    "query": {"type": "string", "required": true}
  },
  "output_schema_fields": {
    "query": {"type": "string", "required": true},
    "hits": {"type": "integer", "required": true}
  },
  "bad_output": [{"title": "API rate limits", "url": "/docs/limits"}],
  "good_output": {"query": "rate limits", "hits": 1}
}
