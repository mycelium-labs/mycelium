{
  "id": "cline-8779-retry",
  "source_url": "https://github.com/cline/cline/issues/8779",
  "source_title": "replace_in_file Tool Fails to Modify Files Despite Reporting Success",
  "component": "ToolRunner",
  "pattern": "Invalid tool args blocked before execution; LLM receives actionable error and retries with corrected arguments.",
  "violation": "missing_required_field",
  "tool_name": "replace_in_file",
  "schema_fields": {
    "path": {"type": "string", "required": true},
    "search": {"type": "string", "required": true},
    "replace": {"type": "string", "required": true}
  },
  "initial_kwargs": {"path": "/workspace/src/a.ts", "search": "foo"},
  "corrected_kwargs": {
    "path": "/workspace/src/a.ts",
    "search": "foo",
    "replace": "bar"
  }
}
