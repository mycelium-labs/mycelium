name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install SDK
        run: pip install -e "./sdk[dev]"

      - name: SDK unit tests
        working-directory: sdk
        run: pytest tests/ -v

      - name: Proof tests
        env:
          PYTHONPATH: sdk
        run: |
          pytest proof/test_proof.py proof/test_proof_af004.py \
            proof/test_proof_af002.py proof/test_proof_af002_task.py -v

      - name: Ruff
        working-directory: sdk
        run: ruff check mycelium tests
